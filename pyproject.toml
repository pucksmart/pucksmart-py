[project]
name = "pucksmart"
version = "0.1"
dependencies = [
    "django==5.0.*",
    "djangorestframework",
    "django-celery-results",
    "celery",
    "markdown",
    "requests",
    "numpy",
    "pandas",
    "matplotlib",
    "seaborn",
]

[tool.setuptools]
packages = [
    "charts",
    "nhlapi",
    "pucksmart",
    "stats",
]

[project.optional-dependencies]
dev = [
  "black",
  "isort",
]

[tool.isort]
profile = "black"